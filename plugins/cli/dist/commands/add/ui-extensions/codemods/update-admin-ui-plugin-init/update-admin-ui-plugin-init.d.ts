import { VendureConfigRef } from '../../../../../shared/vendure-config-ref';
export declare function updateAdminUiPluginInit(vendureConfig: VendureConfigRef, options: {
    pluginClassName: string;
    pluginPath: string;
}): boolean;
